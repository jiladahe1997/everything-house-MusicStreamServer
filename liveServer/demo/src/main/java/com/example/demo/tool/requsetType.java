package com.example.demo.tool;

public enum  requsetType {
    GetAuthRequired,
    Authenticate,
    SetHeartbeat,
    StartStreaming,
    StopStreaming,
    SetSceneItemProperties,
    SetSourceSettings,
    GetSourceSettings,
}
