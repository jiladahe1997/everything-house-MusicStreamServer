package com.example.demo.tool;

public enum  requsetType {
    GetAuthRequired,
    Authenticate,
    StartStreaming,
    StopStreaming,
    SetSceneItemProperties,
    SetSourceSettings,
    GetSourceSettings,
}
